# -Byte-Debugger
A byte debugger is a simple CLI tool that can perform simple tasks such as viewing variables under-the-hood at the byte level. You can even modify them and study how bytes change on variable operations.
